# Dian团队算法考核文档记录

## 3.24

完成1-1任务，利用pytorch工具构建多层感知机并亲手实现softmax层

## 3.25

完成1-2.1，完成训练循环的搭建。
完成步骤：

1. 配置超参数
2. 定义模型，优化器和损失函数
3. 加载数据，定义x，y
4. 对y进行数字编码并把x，y转化为张量
5. 调用模型，搭建训练循环

## 3.26

完成1-2.2，完成对数据训练集和测试集的划分。
完成步骤：

1. 划分训练集和测试集并重新转化为pytorch向量
2. 打印测试结果

完成2-1，完成对标准MHA的实现和向量输出维度的测试
完成步骤

- 初始化部分

1. 定义输入维度，头数量，每个头的维度
2. 定义线性层的Q、K、V，和输出线性层

- 前向传导

1. 获取x形状，初始化Q、K、V
2. 分割成多注意力头
3. 计算出注意力分数
4. 完成掩码，用softmax和dropout部分优化权重
5. 由注意力权重加权得到结果
6. 拼接多个头的结果，线性投影

- 维度测试

1. 设置超参数
2. 调用模型，前向传播
3. 输出结果并比对维度

## 3.27

完成2-2，实现KV Cache并验证序列维度变化
完成步骤：

1. 增加past_key_values参数，由代表K，V的两个张量表示
2. 增加past_key_values的更新逻辑，旧有参数与新参数拼接
3. 构建循环，每次结束后打印k的词数维度，观察增长规律

完成2-3，实现GQA完整统一MHA / GQA / MQA
完成步骤：

1. 根据输入设定K和V的头数，并用repeat_interleave复制
2. 正常计算结果
3. 构建检验函数
